export class footer{
    constructor(page) {
        this.page = page;
        this.footer = ".bottom-0 flex align-items-center justify-center border-2 border-var(--surface-border) border-t sticky bg-gradient-to-b from-white to-gray-300";
    }
}